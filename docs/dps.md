---
title: DPS 排行
---

# DPS 排行

全部数据包单位的 DPS 榜单，可切换**总 DPS** / **对空 DPS** 口径，并可按星球、作者筛选。

::: warning 关于榜单数值
榜单取自外部脚本解析生成的 `index.json`，部分单位（例如作为弹药/子单位存在的 `disrupt-missile`、`anthicus-missile`）的数值偏高是解析口径所致，请结合单位详情页的武器明细一起判断。
:::

<DpsRanking />