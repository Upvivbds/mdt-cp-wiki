---
title: 状态效果总览
---

# 状态效果总览

共 **1** 条。数据包在 `status.*` 下新增或改写的状态效果。

- [余劫](/effects/dynamic) —— `status.dynamic`，修正 生命值 -15%、移动速度 -25%、射速 -3%

> 原版状态（burning、freezing、wet 等）源自 `ContentStatuses`，本 wiki 只收录
> 数据包动过的部分 —— 但作为「对立状态」出现在页面上时仍会标注出来。
